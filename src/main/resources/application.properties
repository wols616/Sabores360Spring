spring.application.name=GestionComida

# --- DataSource (MariaDB) ---
spring.datasource.url=jdbc:mariadb://localhost:3306/sabores360
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# --- JPA/Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# --- Flyway OFF ---
spring.flyway.enabled=false

management.endpoints.web.exposure.include=mappings,health,info
management.endpoint.health.show-details=always

# --- SMTP / Mail (configured with credentials provided) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=n3otech2@gmail.com
spring.mail.password=cjuc xhsk vtmw qzub
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# Optional default from address used by the app:
spring.mail.default-from=n3otech2@gmail.com
# Enable mail debug logging to see mail-sending details in logs (optional)
spring.mail.properties.mail.debug=true

